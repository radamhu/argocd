apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argocd-server-tls
  namespace: argocd
spec:
  commonName: argocd.192.168.0.10.sslip.io
  dnsNames:
    - argocd.192.168.0.10.sslip.io
  issuerRef:
    kind: ClusterIssuer
    name: selfsigned-cluster-issuer
  secretName: argocd-server-tls
