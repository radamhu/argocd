apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: fission-router-tls
  namespace: fission
spec:
  commonName: fission.192.168.0.10.sslip.io
  dnsNames:
    - fission.192.168.0.10.sslip.io
  issuerRef:
    kind: ClusterIssuer
    name: selfsigned-cluster-issuer
  secretName: fission-router-tls
