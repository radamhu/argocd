---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kometa
  labels:
    type: config
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1024M
status: {}
